#include <dirent.h>
#include <pwd.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/stat.h>
#include <sys/types.h>

typedef struct counter {
    size_t dirs;
    size_t files;
} counter_t;

typedef struct entry {
    char *name;
    long inode;
    int device;
    char *mode;
    char *usr_name;
    char *size;
    int is_dir;
    struct entry *next;
} entry_t;

int tree(const char *directory, const char *prefix, counter_t *counter) {
    entry_t *head = NULL, *current, *iter;
    size_t size = 0, index;

    struct dirent *dirent;
    struct stat buf;
    struct passwd *pwd;
    DIR *dir_ptr;

    char *full_path, *segment, *pointer, *next_prefix;

    dir_ptr = opendir(directory);
    if (!dir_ptr) {
        perror("opendir() error!");
        return -1;
    }

    counter->dirs++;

    while ((dirent = readdir(dir_ptr)) != NULL) {
        stat(dirent->d_name, &buf);


        if (dirent->d_name[0] == '.') {
            continue;
        }

        pwd = getpwuid(buf.st_uid);
        if (!pwd) {
            perror("getpwuid() error!");
            return -1;
        }

        current = malloc(sizeof(entry_t));
        current->name =
            strcpy(malloc(strlen(dirent->d_name) + 1), dirent->d_name);
        current->inode = dirent->d_ino;
        current->device = buf.st_dev;
        // strcpy(current->usr_name, file_passwd->pw_name);
        current->usr_name =
            strcpy(malloc(strlen(pwd->pw_name) + 1), pwd->pw_name);
        current->is_dir = dirent->d_type == DT_DIR;
        current->size = buf.st_size;
        current->mode = malloc(sizeof(char) * 20);
        current->next = NULL;

        if (S_ISDIR(buf.st_mode)) {
            current->mode[0] = 'd';
        } else {
            current->mode[0] = '-';
        }

        if (S_IRUSR & buf.st_mode) {
            current->mode[1] = 'r';
        } else {
            current->mode[1] = '-';
        }

        if (S_IWUSR & buf.st_mode) {
            current->mode[2] = 'w';
        } else {
            current->mode[2] = '-';
        }

        if (S_IXUSR & buf.st_mode) {
            current->mode[3] = 'x';
        } else {
            current->mode[3] = '-';
        }

        // Group
        if (S_IRGRP & buf.st_mode) {
            current->mode[4] = 'r';
        } else {
            current->mode[4] = '-';
        }

        if (S_IWGRP & buf.st_mode) {
            current->mode[5] = 'w';
        } else {
            current->mode[5] = '-';
        }

        if (S_IXGRP & buf.st_mode) {
            current->mode[6] = 'x';
        } else {
            current->mode[6] = '-';
        }

        // Others
        if (S_IROTH & buf.st_mode) {
            current->mode[7] = 'r';
        } else {
            current->mode[7] = '-';
        }

        if (S_IWOTH & buf.st_mode) {
            current->mode[8] = 'w';
        } else {
            current->mode[8] = '-';
        }

        if (S_IXOTH & buf.st_mode) {
            current->mode[9] = 'x';
        } else {
            current->mode[9] = '-';
        }

        for (int i = 10; i < 20; i++) {
            current->mode[i] = '\0';
        }

        /**
        int csize = resize(fileInfo.st_size);
        char *tmp;

        switch (csize) {
        case 1: {
            sprintf(tmp, "%d", fileInfo.st_size);
            strcpy(current->size, tmp);
            break;
        }
        case 2: {
            float t = fileInfo.st_size / 1000.0;
            asprintf(tmp, "%g", t);
            strcpy(current->size, &tmp);
            strcat(current->size, 'K');
            break;
        }
        case 3: {
            int t = (int)fileInfo.st_size / 1000;
            sprintf(tmp, "%d", t);
            strcpy(current->size, tmp);
            strcat(current->size, 'K');
            break;
        }
        case 4: {
            float t = (float)fileInfo.st_size / 10000000.0;
            asprintf(&tmp, "%g", t);
            strcpy(current->size, &tmp);
            strcat(current->size, 'M');
            break;
        }
        case 5: {
            int t = (int)fileInfo.st_size / 10000000;
            sprintf(tmp, "%d", t);
            strcpy(current->size, tmp);
            strcat(current->size, 'M');
            break;
        }
        default:
            break;
        }
        **/

        if (head == NULL) {
            head = current;
        } else if (strcmp(current->name, head->name) < 0) {
            current->next = head;
            head = current;
        } else {
            for (iter = head;
                 iter->next && strcmp(current->name, iter->next->name) > 0;
                 iter = iter->next)
                ;

            current->next = iter->next;
            iter->next = current;
        }

        size++;
    }

    closedir(dir_ptr);
    if (!head) {
        return 0;
    }

    for (index = 0; index < size; index++) {
        if (index == size - 1) {
            pointer = "└── ";
            segment = "    ";
        } else {
            pointer = "├── ";
            segment = "│   ";
        }

        printf("%s%s[ %ld %d %s %s %ld ]  %s\n", prefix, pointer, head->inode,
               head->device, head->mode, head->usr_name, head->size,
               head->name);

        if (head->is_dir) {
            full_path = malloc(strlen(directory) + strlen(head->name) + 2);

            sprintf(full_path, "%s/%s", directory, head->name);

            next_prefix = malloc(strlen(prefix) + strlen(segment) + 1);

            sprintf(next_prefix, "%s%s", prefix, segment);

            tree(full_path, next_prefix, counter);
            free(full_path);
            free(next_prefix);
        } else {
            counter->files++;
        }

        current = head;
        head = head->next;

        free(current->name);
        free(current->usr_name);
        free(current->mode);
        free(current);
    }

    return 0;
}

/**
int resize(long size) {
    if (size >= 0 && size < 1000)
        return 1;
    else if (size >= 1000 && size < 10000)
        return 2;
    else if (size >= 10000 && size < 10000000)
        return 3;
    else if (size >= 10000000 && size < 1000000000)
        return 4;
    else if (size >= 1000000000)
        return 5;
    return 0;
}
**/

int main(int argc, char *argv[]) {
    char *directory = argc > 1 ? argv[1] : ".";
    printf("%s\n", directory);

    counter_t counter = {0, 0};
    tree(directory, "", &counter);

    printf("\n%zu directories, %zu files\n",
           counter.dirs ? counter.dirs - 1 : 0, counter.files);
    return 0;
}
